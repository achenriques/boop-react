// en vez de ir importando uno a uno los componentes lo metemos todos aqui
// y importamos el bundle entero.

import Container from './container'
import Header from './header'
import {InputDefault, GrowingInput} from './input'
import {DefaultButton,PrimaryButton} from './buttons'
import Space from './space'
import BackButton from './backButton'
import {DislikeButton,LikeButton,NextButton} from './roundedButton'

export {
  Container,
  Header,
  InputDefault,
  DefaultButton,
  PrimaryButton,
  Space,
  BackButton,
  DislikeButton,
  LikeButton,
  GrowingInput,
  NextButton,
}
